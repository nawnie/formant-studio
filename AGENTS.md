# FORMANT

This is a new standalone project. Physical authority: `D:\Codex-Projects\Desktop\Active Projects\Tools\Formant`.
Do not change any adjacent project or global AI configuration. Use headless tools and loopback only.
Runtime data belongs in ignored `.formant/`; exports cannot write outside its export directory.
The browser and MCP use the same validated command reducer. Keep Web Audio user-gesture requirements visible.
Verify real audio rendering and MCP calls separately from visual review. macOS compatibility is a design target until tested on macOS.
See `docs/DESIGN.md`, `docs/QA.md`, and `README.md` for product and verification notes.
